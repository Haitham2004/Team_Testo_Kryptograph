using System;
using Figgle;

namespace KryptographBibliothek
{
    public class Menue
    {
        public static void Meneu()
        {
            string HauptAusw;
            bool Exit = false;
            bool Hmenue;
            do 
            {
                (int, int) cPosBM = Console.GetCursorPosition();
                Console.WriteLine();
                Console.WriteLine
                    (FiggleFonts.Slant.Render("Kryptographie"));
            }
        }
    }
}
